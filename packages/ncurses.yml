file: "ncurses-6.0.tar.gz"
hash: "f551c24b30ce8bfb6e96d9f59b42fbea30fa3a6123384172f9e7284bcf647260"
uri: "https://ftp.gnu.org/gnu/ncurses/ncurses-6.0.tar.gz"
name: "ncurses"
version: "6.0"
build:
  :cross: "AWS=gawk CPP='cpp -P' $EIR_NCURSES_SOURCE/configure --prefix=$EIR_CROSS_TOOLCHAIN_PREFIX --without-debug && make -C include && make -C progs tic && install -D -v -m755 progs/tic --target-directory $EIR_TOOLCHAIN_PREFIX/bin/"
